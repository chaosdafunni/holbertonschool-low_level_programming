/**
 * _strlen - printing lenght
 *
 * @s: variable used to check
 *
 * Return: ll
 */
#include "main.h"
#include <stdio.h>

int _strlen(char *s)

{
	size_t ll = 0;

	while (*s++)

	ll++;

	return (ll);

}
